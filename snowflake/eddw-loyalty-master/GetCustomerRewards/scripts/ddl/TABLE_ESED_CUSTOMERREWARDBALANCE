USE Database <<EDM_DB_NAME_R>>;
USE Schema DW_R_LOYALTY;

create or replace TABLE ESED_CUSTOMERREWARDBALANCE (
	FILENAME VARCHAR(5000),
	SRC_XML VARIANT,
	CREATED_TS TIMESTAMP_LTZ(9) DEFAULT CURRENT_TIMESTAMP()
);
